# Generated by pack-dist.ps1 on every run: derived from bin\Release\ContentTool, and the one
# folder in the tree whose stale copy could be uploaded by mistake.
Dist/

# The SteamworksPy runtime: environment-local native binaries and a vendored package, recreated
# per machine (steamugc\README.md). Never committed.
steamugc/steamworks/
steamugc/*.dll
steamugc/steam_appid.txt
