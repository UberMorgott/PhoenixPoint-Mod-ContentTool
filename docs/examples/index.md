# Worked examples

TODO(phase 3): Replace this landing page with a worked explanation of every public demo.

Phase 3 will document what each demo proves, which source and manifest technique achieves each
feature, the commands and success output, and the matching Phase 2 recipe. The demos waiting for
that treatment are:

```text
demos\
  MenuMusic\
    meta.json               <- each demo is an installable mod project
    ppcontent.json
  ReplaceUiSounds\
    meta.json
    ppcontent.json
  ...                       <- remaining projects are listed below
```

- `MenuMusic`
- `ReplaceUiSounds`
- `AddUiSounds`
- `IntroVideo`
- `QuitCutscene`
- `WeaponMesh`
- `MaterialTweak`
- `CustomCreature`
- `HumanoidSoldier`
- `ReplaceCharacterBody`
- `WeaponAdd`

`NoDepTexture` is not a feature demo. It is a dependency measurement fixture and will remain outside
the public worked-example set.
